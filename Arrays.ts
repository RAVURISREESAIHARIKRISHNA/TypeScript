// Syntax
//  let iddentifier : datatype[] = [....];
let myArray : number[] = [100,200,300];

for(let i=0;i<=myArray.length-1;i++){
    console.log(i+" => "+myArray[i]);
}

// Syntax
//  let identifier : Array<datatype> = [....]
let myArray2 : Array<number> = [400,500,600,700,8000];

for(let i=0;i<=myArray2.length-1;i++){
    console.log(i+" => "+myArray2[i]);
    
}
